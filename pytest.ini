[pytest]
testpaths = tests
addopts = -q --strict-markers
# Safety net: a GUI test that opens a modal dialog would otherwise hang the
# suite forever instead of failing.
timeout = 120
timeout_method = thread
