[pytest]
testpaths = tests
addopts = -q --strict-markers
# Rete di sicurezza: un test GUI che apre una finestra modale bloccherebbe
# la suite per sempre invece di fallire.
timeout = 120
timeout_method = thread
