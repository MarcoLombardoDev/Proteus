# Development and test dependencies.
# Install the runtime ones first:  pip install -r requirements.txt
-r requirements.txt

pytest>=7.4

# Used only to build and re-open real Office documents in the tests. Proteus
# itself reads and writes those packages with the standard library alone.
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1
# Prevents a GUI test that opens a modal dialog from hanging the suite.
pytest-timeout>=2.1

# Screen capture for docs/generate_screenshots.py. Not needed to run or
# test Proteus, only to regenerate the README images.
mss>=9.0

# Parses .github/workflows/*.yml so a broken workflow file is caught by the
# test suite instead of by the next push failing silently on GitHub.
pyyaml>=6.0
